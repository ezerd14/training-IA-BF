services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: backend-app
    environment:
      JWT_SECRET_KEY: change-me-in-production
    ports:
      - "8000:8000"
